import { View, Text } from "react-native";
import React from "react";

const Footer = () => {
  return (
    <View className="mt-10">
      <Text>Footer</Text>
    </View>
  );
};

export default Footer;
